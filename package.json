{
	"license": "MIT",
	"dependencies": {
    	"autoprefixer": "^10.2.0",
    	"postcss": "^8.2.2",
    	"postcss-import": "^14.0.0",
		"tailwindcss": "^2.0.2"
	},
 	"scripts": {
		"build": "bundle exec jekyll build && find ./_site/styles/* ! -name 'main.css' -exec rm -rf {} + && purgecss --css './_site/styles/main.css' --content './_site/**/*.html' './_site/**/*.js' --output './_site/styles/main.css'",
		"serve": "bundle exec jekyll serve --livereload"
	}
}
